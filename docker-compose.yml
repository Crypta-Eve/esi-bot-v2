version: "3.7"

services:
    bot:
        image: docker.pkg.github.com/eveisesi/esi-bot-v2/esi-bot:${DOCKER_VERSION}
        network_mode: "host"
        restart: on-failure
        command: ./bot
        container_name: bot
        hostname: bot
        env_file:
            - .env
